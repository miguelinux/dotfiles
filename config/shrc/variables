# -*- mode: shell-script; indent-tabs-mode: nil; sh-basic-offset: 4; -*-
# ex: ts=8 sw=4 sts=4 et filetype=sh
#
# SPDX-License-Identifier: GPL-3.0-or-later

PATH=${PATH}:${HOME}/.local/bin

BAT_PAGER=""

#if test -d /usr/lib/jvm/java-1.8.0-openjdk
#then
#    PYCHARM_JDK=/usr/lib/jvm/java-1.8.0-openjdk
#    export PYCHARM_JDK
#fi

#if test -d /usr/lib/jvm/java-11-openjdk
#then
#    PYCHARM_JDK=/usr/lib/jvm/java-11-openjdk
#    export PYCHARM_JDK
#fi

if [ -d /home/miguel/.opencode/bin ]
then
  # opencode
  PATH=/home/miguel/.opencode/bin:$PATH
fi

# Added by QGenie for OpenCode
if test -d $HOME/.local/share/qgenie-cli/opencode-cli/bin
then
    PATH=$HOME/.local/share/qgenie-cli/opencode-cli/bin:$PATH
fi

if [ "x${DISPLAY}" != "x" ]; then
    EDITOR="gvim -f"
    VISUAL="gvim -f"
else
    EDITOR="vim "
    VISUAL="vim "
fi

if [ "${XDG_SESSION_TYPE}" = "wayland" ]
then
    export GVIM_ENABLE_WAYLAND=1
fi

export PATH EDITOR VISUAL BAT_PAGER
